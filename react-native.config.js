module.exports = {
  project: {
    android: {},
  },
  // No custom bundled fonts: the app renders in the system font (Roboto on Android),
  // which is reliable across devices. Inter can be added later via an ./assets/fonts
  // folder + `npx react-native-asset` if desired.
};
